const GroupPageHeaderStyles = theme => ({
  root: {
    marginBottom: theme.spacing(8),
  },
  textField: {
    ...theme.typography.h1,
    color: theme.palette.common.black,
  },
});

export default GroupPageHeaderStyles;
