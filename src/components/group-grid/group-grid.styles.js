const GroupGridStyles = theme => ({
  circularProgressContainer: {
    display: 'flex',
    flexDirection: 'column',
    justifyContent: 'center',
    alignItems: 'center',
    padding: theme.spacing(2),
  },
});

export default GroupGridStyles;
